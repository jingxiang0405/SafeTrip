import db from "#src/database.js";

// for example purpose, querying all users
async function Example() {
    let result;
    try {


    } catch (err) {
        console.log(err);
    }

    return result;
}

export {
    Example,
}
